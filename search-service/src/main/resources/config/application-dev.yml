spring:
  profiles:
    active: dev

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: false

logging:
  level:
    at.alladin.nettest: DEBUG

search-service:
  elasticsearch:
    host: localhost
    port: 19200
    scheme: http
    index: nntool
    queryTimeout: 10s
