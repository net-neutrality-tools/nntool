import { Component } from '@angular/core';

@Component({
  selector: 'nntool-section-map',
  template: `
    <section class="section" id="section-map">
      <ng-content></ng-content>
    </section>
  `
})
export class SectionMapComponent {}
