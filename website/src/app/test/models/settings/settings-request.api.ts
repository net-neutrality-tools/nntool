import {BasicRequestAPI} from '../basic-request.api';

export interface SettingsRequestAPI extends BasicRequestAPI {
    dummy?: string; // linter: An interface declaring no members is equivalent to its supertype.
}

