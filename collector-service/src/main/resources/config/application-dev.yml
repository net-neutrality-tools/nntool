spring:
  profiles:
    active: dev

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: false

  couchdb:
    connection:
      url: http://localhost:5984
      #username:
      #password:
    databases:
      - name: measurement
        create-if-not-exists: true
        entities:
          - CouchDbMeasurement

#  messages:
#    cache-seconds: 1 # short cache for development

#  jackson:
#    serialization:
#      indent_output: true

#management:
#  trace:
#    include: request-headers,response-headers,errors # Items to be included in the trace.

server:
  port: 8081

logging:
  level:
    at.alladin.nettest: DEBUG
